---
// Foundation, edit with care
//
// THE STYLEGUIDE WALL - the fixed-data page the visual-regression suite shoots.
//
// WHY IT EXISTS. Three defects shipped in one afternoon on a repo forked from
// this starter that every automated gate was structurally blind to: the footer
// still carried the FORKED REPO'S logo, so another client's wordmark sat at the
// bottom of every page; a base-layer rule pinned every heading to a token meant
// for dark grounds, so in light mode the whole site had cream headings on
// paper; and a card rule declared before its base rule at equal specificity
// silently lost its background, leaving its heading at 1.15:1. Types passed,
// every unit test passed, axe passed. (A text-shadow makes axe report contrast
// as "incomplete" rather than a violation, and the suite asserts on
// violations.) All three were found by a human looking at the site.
//
// A screenshot diff is the only gate that can see that class of bug, and it
// needs FIXED data or it cries wolf: real pages carry content that changes, so
// shooting them would churn the baseline on words rather than on design.
// Everything below is hardcoded here on purpose. If a pixel moves, the design
// moved.
//
// WHAT BELONGS HERE: anything whose appearance is decided by the design system
// rather than by content. The palette tokens, the type scale on each surface,
// the semantic shadcn pairs, the CTA variants, and the polish-layer utilities.
// The header and footer come free from BaseLayout, which is what would have
// caught the inherited logo.
//
// WHAT DOES NOT: anything time-based, anything read from Sanity, anything that
// grows. A project adding its own signature objects to the wall should give
// them fixed props, right here, and nowhere else.
//
// noindex, and filtered out of the sitemap in astro.config.mjs. It is a real
// route so Playwright can load it from the built output, but it is not for
// visitors, and 'styleguide' is reserved in src/lib/reservedSlugs.ts so a
// custom page cannot shadow it.

import BaseLayout from '@/layouts/BaseLayout.astro';
import SectionHeading from '@/components/SectionHeading.astro';
import CtaLink from '@/components/CtaLink.astro';
import { site } from '@/data/site';
import { surfaceClass } from '@/lib/surfaces';
// Church identity primitives (2026-09-23, the Who We Are "alive" pass).
import ArchFrame from '@/components/church/ArchFrame.astro';
import BuildingGlyph from '@/components/church/BuildingGlyph.astro';
import WatchwordMark from '@/components/church/WatchwordMark.astro';
import SeasonLine from '@/components/church/SeasonLine.astro';
// scaffold: church
// The church blocks (Task 6, plus link cards from task 5b), fixtured with real
// church content so a pixel diff here means the design moved, not the content.
// See the fixture data block below for why each value is what it is.
import SundayTimes from '@/components/sections/SundayTimes.astro';
import Timeline from '@/components/sections/Timeline.astro';
import StaffGrid from '@/components/sections/StaffGrid.astro';
import FaqBand from '@/components/sections/FaqBand.astro';
import ScriptureBand from '@/components/sections/ScriptureBand.astro';
import HeritageBand from '@/components/sections/HeritageBand.astro';
import GiveBand from '@/components/sections/GiveBand.astro';
import DocumentList from '@/components/sections/DocumentList.astro';
import LinkCards from '@/components/sections/LinkCards.astro';
import Hours from '@/components/sections/Hours.astro';
// scaffold:end
// Task 7: temporary hero fixtures (frames, layout, facts). No hero with frames
// exists in the dataset yet (plan 2b seeds one) so these two calls are the
// only way to see the cross-fade and the split layout render before content
// lands. Remove once plan 2b's seeded hero gives the styleguide (and Playwright)
// something real to shoot instead.
import Hero from '@/components/Hero.astro';

/** The brand palette from the `@theme` block in src/styles/globals.css. */
const BRAND_TOKENS = [
  '--color-primary',
  '--color-primary-dark',
  '--color-accent',
  '--color-accent-dark',
  '--color-secondary',
  '--color-tertiary',
  '--color-bg',
  '--color-bg-soft',
  '--color-border-soft',
];

/** The shadcn semantic pairs. These are the ones that must flip with theme. */
const SEMANTIC_TOKENS = [
  '--background',
  '--foreground',
  '--card',
  '--card-foreground',
  '--muted',
  '--muted-foreground',
  '--primary',
  '--primary-foreground',
  '--border',
];

// Written out rather than built from a loop variable: Tailwind's scanner reads
// SOURCE TEXT, so `text-${h}` generates nothing at all and the whole scale
// would render at the base size in a screenshot nobody would question.
const HEADINGS = [
  ['Display h1', 'text-h1'],
  ['Display h2', 'text-h2'],
  ['Display h3', 'text-h3'],
  ['Display h4', 'text-h4'],
  ['Display h5', 'text-h5'],
  ['Display h6', 'text-h6'],
] as const;

/** Fixed CTA stand-ins. Never read from Sanity: that is the whole point. */
const PRIMARY_CTA = { label: 'Plan your visit', linkType: 'external' as const, externalUrl: '#' };
const SECONDARY_CTA = { label: 'Watch a service', linkType: 'external' as const, externalUrl: '#' };

// =============================================================================
// Church identity fixtures (2026-09-23)
// =============================================================================
/** The identity palette, swatched in both themes (every one flips in .dark). */
const IDENTITY_TOKENS = [
  '--color-season-green',
  '--color-season-violet',
  '--color-season-purple',
  '--color-season-red',
  '--color-season-gold',
  '--color-season-white',
  '--color-goal-green',
  '--color-goal-gold',
  '--color-goal-purple',
  '--color-goal-brown',
  '--color-goal-ink-on-gold',
  '--color-mint',
  '--color-gold-light',
];

// One FIXED date per season, so the season line is part of the baseline and
// never moves with the build date. Each sits on its own band colour, written
// out as a literal class (Tailwind reads source text).
const SEASON_FIXTURES = [
  { date: new Date('2026-12-06T12:00:00Z'), band: 'bg-season-violet' },
  { date: new Date('2026-12-28T12:00:00Z'), band: 'bg-season-white' },
  { date: new Date('2027-01-20T12:00:00Z'), band: 'bg-season-green' },
  { date: new Date('2027-03-01T12:00:00Z'), band: 'bg-season-purple' },
  { date: new Date('2027-04-10T12:00:00Z'), band: 'bg-season-gold' },
  { date: new Date('2027-05-16T12:00:00Z'), band: 'bg-season-red' },
  { date: new Date('2026-09-23T12:00:00Z'), band: 'bg-season-green' },
];

// Real photographs from this project's own library (the church's own blog),
// the ones the prototype framed or their nearest library twin, each with a
// hotspot so the frame's crop follows it. Alt text is the library's own; no
// names are attached.
const ARCH_FIXTURES = {
  girls: {
    asset: { _ref: 'image-bad93d7a2b560248a8b1b249154c4bbc1ed83af5-1600x1064-jpg' },
    hotspot: { x: 0.66, y: 0.4 },
    alt: 'Two smiling girls, one with red hair, pose together in front of colorful stained glass windows.',
  },
  coneflowers: {
    asset: { _ref: 'image-4f64da601a5d01fae1fa8ac50a3898207b6e7f24-2400x1600-jpg' },
    hotspot: { x: 0.68, y: 0.4 },
    alt: 'A young girl smiles outdoors, standing among tall purple coneflowers in a garden.',
  },
  bibles: {
    asset: { _ref: 'image-50fe129b92b4673a51bf94b9408c5c8a22346ce4-2400x1136-jpg' },
    hotspot: { x: 0.5, y: 0.45 },
    alt: 'Three girls sitting together in a pew, smiling, with open Bibles on their laps.',
  },
  balcony: {
    asset: { _ref: 'image-d7aa65ea099b67f8adb73eb7f5bed6855f907133-2400x1714-jpg' },
    alt: 'A man and a woman smiling together in the sanctuary balcony, arched windows and pews visible behind them.',
  },
};
const RULE_CTA = { label: 'Plan your visit', linkType: 'external' as const, externalUrl: '#' };
const RULE_CTA_DARK = {
  label: 'Praise & Proclaim, our watchword',
  linkType: 'external' as const,
  externalUrl: '#',
};

// scaffold: church
// =============================================================================
// Church block fixtures (Task 6)
// =============================================================================
// Real church content, never lorem: the words themselves are what a
// visual-regression baseline is defending, so a placeholder string here would
// let a real content-shaped bug (a heading colliding with a long word, a
// button label wrapping) hide behind "it's just lorem ipsum". Every fact below
// is FBCM's own: the Sunday schedule, the three doors, the FAQ answers
// (verbatim from scripts/data/pages/faq-entries.json), Isaiah 12:4, the
// building's 1929 date, the giving URL, and the three real publications.
//
// The one deliberate exception is the staff grid: three SYNTHETIC people
// (never real staff or real photos), because that block's data comes from
// GROQ in production and the styleguide must stay fixed regardless of who is
// actually on staff. NO PHOTO IS SET on any of them, on purpose: the two
// candidates were tried and rejected. The 404 page's own fallback ref
// (src/pages/404.astro) is a leftover from the FORKED starter's demo project
// (CLAUDE.md rule 11, "check for residue") and this repo's real, configured
// Sanity project (7jw947g5) has never heard of it, so it 404s and paints a
// browser's broken-image icon rather than a designed state. Every real asset
// actually IN this project's library was checked by eye (161 assets, all
// imported from the church's own blog) and every one is a photograph of
// identifiable people at a real event, which is wrong to attach to a fake
// name and a fake "Pastor" title even captioned as a fixture. StaffGrid.astro
// already has a real, theme-aware, designed answer for "no photo yet" -- the
// aspect-[4/5] bg-surface-soft box under the gold rule -- so leaving `photo`
// undefined uses the component's own fallback rather than inventing a new one.

/** One-paragraph portable text block, for the two fields typed as rich text. */
function paragraph(key: string, text: string) {
  return [
    {
      _type: 'block',
      _key: key,
      style: 'normal',
      markDefs: [],
      children: [{ _type: 'span', _key: `${key}-s1`, text }],
    },
  ];
}

const SUNDAY_TIMES_FIXTURE = {
  eyebrow: 'This Sunday',
  heading: 'When we gather',
  items: [
    {
      _key: 'st-1',
      label: 'Sunday school',
      big: '9:30 am',
      body: 'Classes for all ages meet before worship.',
    },
    {
      _key: 'st-2',
      label: 'Donut hour',
      big: '10:15 am',
      body: 'Coffee and donuts in the fellowship hall, a relaxed way to meet people before the service.',
    },
    {
      _key: 'st-3',
      label: 'Worship',
      big: '10:45 am',
      body: 'The main service, in the sanctuary.',
    },
  ],
  doors: [
    {
      _key: 'door-1',
      name: 'Adams Street circular drive',
      body: 'The accessible entrance. An automatic door opener sits beside the mailbox.',
    },
    {
      _key: 'door-2',
      name: 'Front doors',
      body: 'The large wooden doors open straight into the sanctuary.',
    },
    {
      _key: 'door-3',
      name: 'Jefferson Street side doors',
      body: 'Near the church offices, on the Jefferson Street side of the building.',
    },
  ],
  showMap: true,
};

const TIMELINE_FIXTURE = {
  eyebrow: 'A Sunday morning',
  heading: 'How the morning runs',
  rows: [
    {
      _key: 'tl-1',
      marker: '9:30 am',
      title: 'Sunday school',
      body: paragraph('tl-1-p1', 'Classes for all ages meet before worship.'),
    },
    {
      _key: 'tl-2',
      marker: '10:15 am',
      title: 'Donut hour',
      body: paragraph(
        'tl-2-p1',
        'Coffee and donuts in the fellowship hall, a relaxed way to meet people before the service.',
      ),
    },
    {
      _key: 'tl-3',
      marker: '10:45 am',
      title: 'Worship',
      body: paragraph('tl-3-p1', 'The main service, in the sanctuary.'),
    },
    {
      _key: 'tl-4',
      marker: 'First Sunday',
      title: 'Communion',
      body: paragraph('tl-4-p1', 'Communion is observed on the first Sunday of each month.'),
    },
  ],
};

const STAFF_GRID_FIXTURE = {
  eyebrow: 'Who you will meet',
  heading: 'Staff',
  group: 'all',
  showBios: true,
  members: [
    {
      _id: 'staff-1',
      name: 'First Last',
      slug: 'first-last',
      role: 'Pastor',
      email: 'placeholder@example.com',
      group: 'pastors',
      order: 1,
      bio: paragraph('staff-1-bio', 'Synthetic bio text for the styleguide fixture only.'),
    },
    {
      _id: 'staff-2',
      name: 'First Last',
      slug: 'first-last-2',
      role: 'Church Coordination Team',
      email: 'placeholder@example.com',
      group: 'coordination',
      order: 1,
      bio: paragraph('staff-2-bio', 'Synthetic bio text for the styleguide fixture only.'),
    },
    {
      _id: 'staff-3',
      name: 'First Last',
      slug: 'first-last-3',
      role: 'Volunteer Coordinator',
      email: 'placeholder@example.com',
      group: 'support',
      order: 1,
      bio: paragraph('staff-3-bio', 'Synthetic bio text for the styleguide fixture only.'),
    },
  ],
};

// Verbatim from scripts/data/pages/faq-entries.json (sortOrder 7 and 10).
const FAQ_FIXTURE = {
  eyebrow: 'Questions',
  heading: 'Questions and answers',
  items: [
    {
      _key: 'faq-1',
      question: 'What should I wear?',
      answer: paragraph(
        'faq-1-p1',
        'Casual dress is welcome. People wear a variety of styles of clothes from short sleeves (especially in the summer), to suit jackets and “Sunday dresses.” Please dress comfortably and in a way which allows you to devote attention to worship.',
      ),
    },
    {
      _key: 'faq-2',
      question: 'Am I allowed to take the Lord’s Supper (communion?)',
      answer: paragraph(
        'faq-2-p1',
        'We believe it is the Lord’s table, and all who believe in him and have been baptized may partake. This includes believing Christians, no matter their particular denomination.',
      ),
    },
  ],
};

const SCRIPTURE_FIXTURE = {
  verse:
    'Give praise to the Lord, proclaim his name; make known among the nations what he has done',
  reference: 'Isaiah 12:4',
  accentWord: 'proclaim',
};

// A real photo from FBCM's own library (already public: imported from the
// church's own blog), chosen deliberately over the other 160 assets in the
// project because it is the one that actually shows the BUILDING -- the
// sanctuary's arches and its stained glass window -- rather than a close-up
// portrait. The congregation is visible only from behind, which is why this
// one and not another was the right choice: see the fixture-data comment
// above for the two options this ruled out.
const HERITAGE_FIXTURE = {
  eyebrow: 'Our building',
  heading: 'Since 1929',
  body: 'The limestone building on the corner of Adams and Jefferson has been the congregation’s home since 1929.',
  image: {
    asset: { _ref: 'image-e6666c121c6318a9ce9f82798fefbfe16d6ac9d4-4032x1908-jpg' },
    alt: 'Inside the sanctuary, looking toward the stained glass window.',
  },
};

// Task 7: temporary hero fixtures. Three real FBCM photos (from this
// project's own asset library, same source as HERITAGE_FIXTURE above) for the
// full-bleed cross-fade, and one for the split layout with three facts.
const HERO_FULL_FIXTURE = {
  eyebrow: 'Welcome home',
  headline: 'A church for the whole of Muncie',
  subhead: 'Join us this Sunday for worship, fellowship, and a warm welcome.',
  frames: [
    {
      asset: { _ref: 'image-e6666c121c6318a9ce9f82798fefbfe16d6ac9d4-4032x1908-jpg' },
      alt: 'Inside the sanctuary, looking toward the stained glass window.',
    },
    {
      asset: { _ref: 'image-25c93a161fc4fbb25f3b87f80522315e3596ea70-1159x643-jpg' },
      alt: 'A styleguide fixture photo of the church.',
    },
    {
      asset: { _ref: 'image-4a5061d1625f61322237a6950c9bd2d6f819edba-1900x1400-jpg' },
      alt: 'A styleguide fixture photo of the church.',
    },
  ],
  // Buttons on both heroes, so the styleguide shows the church pair the way a
  // real hero does: gold fill with an indigo-field label for the primary, the
  // outlined variant beside it (CLAUDE.md rule 17).
  primaryCta: { label: 'Plan your visit', linkType: 'external' as const, externalUrl: '#' },
  secondaryCta: { label: 'Watch a service', linkType: 'external' as const, externalUrl: '#' },
};

const HERO_SPLIT_FIXTURE = {
  eyebrow: 'This week',
  headline: 'Come as you are',
  subhead: 'One service, one family, every Sunday morning.',
  frames: [
    {
      asset: { _ref: 'image-25c93a161fc4fbb25f3b87f80522315e3596ea70-1159x643-jpg' },
      alt: 'A styleguide fixture photo of the church.',
    },
  ],
  facts: [
    { _key: 'fact-1', label: 'Sundays', value: '10:45 am' },
    { _key: 'fact-2', label: 'Where', value: '309 E Adams St' },
    { _key: 'fact-3', label: 'Online', value: 'Facebook Live' },
  ],
  // Buttons on both heroes, so the styleguide shows the church pair the way a
  // real hero does: gold fill with an indigo-field label for the primary, the
  // outlined variant beside it (CLAUDE.md rule 17).
  primaryCta: { label: 'Plan your visit', linkType: 'external' as const, externalUrl: '#' },
  secondaryCta: { label: 'Service times', linkType: 'external' as const, externalUrl: '#' },
};

const GIVE_BAND_FIXTURE = {
  heading: 'Give',
  body: 'Give online, in the offering plate on Sunday, or by mail.',
  buttonLabel: 'Give through Church Center',
  buttonUrl: 'https://fbcmuncie.churchcenter.com/giving',
};

const DOCUMENT_LIST_FIXTURE = {
  eyebrow: 'Resources',
  heading: 'Documents',
  docs: [
    { _key: 'doc-1', title: 'The Visitor', year: 2025, note: 'Quarterly newsletter' },
    { _key: 'doc-2', title: 'We Are the Clay', year: 2019 },
    { _key: 'doc-3', title: 'Constitution and bylaws', note: 'Governing documents' },
  ],
};

// Three cards, because three is the shape both pages that use this block ask
// for (spec 5.1 item 4 and 5.3 item 7) and the one where the lg:grid-cols-3
// branch is what gets shot. The links point at the three pages the Home band
// really points at, written as same-site paths rather than references: the
// styleguide never reads Sanity, so a reference here would resolve to nothing.
// The hours band reads everything it prints off siteSettings (Hours.astro, and
// CLAUDE.md rule 15), so the fixture is a settings OBJECT rather than a set of
// block fields. Passing it explicitly also keeps the styleguide off the live
// dataset: the component only falls back to getSiteSettings() when no prop is
// given, and a visual baseline that moves when the church edits its Friday
// closing time is not a baseline. The values are FBCM's own, as seeded.
const HOURS_FIXTURE = {
  eyebrow: 'Office',
  heading: "Office and pastors' hours",
  settings: {
    address: '309 East Adams Street\nMuncie, IN 47305',
    officeHours: [
      {
        _type: 'block' as const,
        _key: 'oh-1',
        style: 'normal',
        children: [
          {
            _type: 'span' as const,
            _key: 'oh-1-0',
            text: 'Monday to Thursday: 9 am to 12 pm and 1 pm to 4 pm',
          },
        ],
      },
      {
        _type: 'block' as const,
        _key: 'oh-2',
        style: 'normal',
        children: [{ _type: 'span' as const, _key: 'oh-2-0', text: 'Friday: 9 am to 12 pm' }],
      },
      {
        _type: 'block' as const,
        _key: 'oh-3',
        style: 'normal',
        children: [{ _type: 'span' as const, _key: 'oh-3-0', text: 'Sunday: 9 am to 12 pm' }],
      },
    ],
    pastoralHours: [
      {
        _type: 'block' as const,
        _key: 'ph-1',
        style: 'normal',
        children: [
          {
            _type: 'span' as const,
            _key: 'ph-1-0',
            text: 'Tuesdays are set aside for meetings with members and visitors.',
          },
        ],
      },
    ],
  },
};

const LINK_CARDS_FIXTURE = {
  eyebrow: 'First Baptist Muncie',
  heading: 'A Baptist church in the heart of downtown since 1859.',
  cards: [
    {
      _key: 'lc-1',
      _type: 'linkCard' as const,
      title: 'What we believe',
      body: 'We hold to the Bible, to baptism on a person’s own profession of faith, and to the freedom of each church to govern itself.',
      cta: {
        label: 'Our beliefs',
        _type: 'ctaBlock' as const,
        linkType: 'external' as const,
        externalUrl: '/beliefs',
        openInNewTab: false,
      },
    },
    {
      _key: 'lc-2',
      _type: 'linkCard' as const,
      title: 'How we serve',
      body: 'Sunday school, music, youth, and work with partners across Muncie and beyond.',
      cta: {
        label: 'Ministries',
        _type: 'ctaBlock' as const,
        linkType: 'external' as const,
        externalUrl: '/ministries',
        openInNewTab: false,
      },
    },
    {
      _key: 'lc-3',
      _type: 'linkCard' as const,
      title: 'Where we’ve been',
      body: 'The congregation has met in downtown Muncie since 1859, and in this building since 1929.',
      cta: {
        label: 'Our history',
        _type: 'ctaBlock' as const,
        linkType: 'external' as const,
        externalUrl: '/history',
        openInNewTab: false,
      },
    },
  ],
};
// scaffold:end
---

<BaseLayout
  title={`Styleguide | ${site.name}`}
  description="Fixed-data design system wall. Not for visitors."
  noindex
>
  <section class="bg-background py-section-lg">
    <div class="mx-auto w-full max-w-content px-6">
      <SectionHeading
        headingId="styleguide-heading"
        level="h1"
        eyebrow="Design system"
        headline="Styleguide"
        subhead="Fixed data. Every pixel below is decided by the design system, so a diff here means the design moved, not the content."
      />

      <h2 class="mt-14 font-display text-h3">Brand palette</h2>
      <ul class="mt-5 grid gap-3 sm:grid-cols-3 lg:grid-cols-5">
        {
          BRAND_TOKENS.map((t) => (
            <li class="rounded-lg border border-border p-3">
              <span
                class="block h-12 w-full rounded border border-foreground/20"
                style={`background: var(${t})`}
              />
              <code class="mt-2 block font-mono text-[0.6875rem] break-all">{t}</code>
            </li>
          ))
        }
      </ul>

      <h2 class="mt-14 font-display text-h3">Semantic tokens</h2>
      <p class="mt-2 max-w-[60ch] text-sm text-muted-foreground">
        These are the pairs that must flip with the theme. A swatch that looks identical in both
        baselines is a token that forgot to.
      </p>
      <ul class="mt-5 grid gap-3 sm:grid-cols-3 lg:grid-cols-5">
        {
          SEMANTIC_TOKENS.map((t) => (
            <li class="rounded-lg border border-border p-3">
              <span
                class="block h-12 w-full rounded border border-foreground/20"
                style={`background: var(${t})`}
              />
              <code class="mt-2 block font-mono text-[0.6875rem] break-all">{t}</code>
            </li>
          ))
        }
      </ul>

      <h2 class="mt-14 font-display text-h3">Type, on the page ground</h2>
      <div class="mt-5">
        {HEADINGS.map(([label, cls]) => <p class={`font-display ${cls}`}>{label}</p>)}
        <p class="mt-3 max-w-[60ch]">
          Body copy on the page ground. The quick brown fox jumps over the lazy dog, twice, so the
          line has enough letters in it for a rasterisation change to show.
        </p>
        <p class="mt-2 text-sm text-muted-foreground">Muted body copy, one step down.</p>
        {
          /* BUG FOUND WHEN TASK 6 ADDED THIS PAGE TO THE AXE SWEEP (it had never
             been route-listed before): both lines below used the FIXED brand-
             mirror tokens (--color-primary / --color-primary-dark), which never
             flip with .dark and exist for text on FIXED grounds (gold, the ink
             band, hover states -- see the comment on --color-primary in
             globals.css). Applied directly to the page ground, which DOES flip,
             they read fine in light and at ~1.1-1.3:1 in dark. The real pattern
             for an eyebrow and a link ON THE PAGE GROUND is the theme-aware
             pair SectionHeading.astro and Footer.astro already use:
             text-foreground/80 and text-link. */
        }
        <p class="mt-4 font-mono text-[0.75rem] tracking-eyebrow text-foreground/80 uppercase">
          Eyebrow, uppercase, tracked
        </p>
        <p class="mt-4">
          A <a class="nav-underline text-link" href="#">link with the nav underline</a> in a sentence,
          so its rule sits against real type.
        </p>
      </div>

      <h2 class="mt-14 font-display text-h3">Type, on a card</h2>
      {
        /* The exact combination that failed elsewhere: a heading inside a card,
           where a base-layer heading colour would otherwise win over the card's
           own foreground token. */
      }
      <div class="mt-5 max-w-[30rem] rounded-lg border border-border bg-card p-7">
        <h3 class="font-display text-h3 text-card-foreground">Heading on a card</h3>
        <p class="mt-2 text-sm text-card-foreground">Body copy on a card.</p>
        <p class="mt-2 text-sm text-muted-foreground">Muted copy on a card.</p>
      </div>

      <h2 class="mt-14 font-display text-h3">Calls to action</h2>
      <div class="mt-5 flex flex-wrap items-center gap-4">
        <CtaLink cta={PRIMARY_CTA} variant="gold" />
        <CtaLink cta={SECONDARY_CTA} variant="outline" />
      </div>
    </div>
  </section>

  {
    /* The alternating surface, with a CTA pair on it, so both grounds are shot.
     BUG FOUND IN TASK 1: this section carried `surface-warm` (a gradient-only
     overlay utility, see globals.css) with no `bg-*` class at all, so it never
     painted the muted surface it claims to demonstrate -- it was rendering the
     page background underneath. surfaceClass('muted') is the real class the
     cadence assigns (bg-surface-soft: a taupe tint over cream in light, #262548
     in dark), fixed here rather than relaxed away. */
  }
  <section class={`${surfaceClass('muted')} surface-warm py-section-lg`}>
    <div class="mx-auto w-full max-w-content px-6">
      <SectionHeading
        headingId="styleguide-surface"
        level="h2"
        eyebrow="The other ground"
        headline="Soft paper surface"
        headingAccent="Soft"
        subhead="The alternating band the section cadence puts every other section on."
        align="center"
      />
      <div class="mt-8 flex flex-wrap items-center justify-center gap-4">
        <CtaLink cta={PRIMARY_CTA} variant="gold" class="press-tactile" />
        <CtaLink cta={SECONDARY_CTA} variant="outline" />
      </div>
    </div>
  </section>

  {
    /* A dark panel, because the inverse tone has its own contrast problems and
      they are invisible in a light-only shot. */
  }
  <section class="bg-accent-dark py-section-lg">
    <div class="mx-auto w-full max-w-content px-6">
      <SectionHeading
        headingId="styleguide-inverse"
        level="h2"
        eyebrow="Inverse"
        headline="Type on a dark panel"
        subhead="The closing-CTA ground. Its foreground tokens are separate from the page's."
        tone="inverse"
        align="center"
      />
      <div class="mt-8 flex flex-wrap items-center justify-center gap-4">
        <CtaLink cta={PRIMARY_CTA} variant="gold" onDark />
        <CtaLink cta={SECONDARY_CTA} variant="outline" onDark />
      </div>
    </div>
  </section>

  {
    /* Church identity (2026-09-23): the primitives the Who We Are "alive"
       page is built from, each against the band it is made for. */
  }
  <section class="bg-background py-section-lg" aria-labelledby="styleguide-identity">
    <div class="mx-auto w-full max-w-content px-6">
      <h2 id="styleguide-identity" class="font-display text-h3">Church identity</h2>
      <p class="mt-2 max-w-[60ch] text-sm text-muted-foreground">
        The season and goal bands, the two arches drawn from the building, the four building glyphs,
        the watchword mark and the rule button. Every text pair here is measured in both themes in
        theme-tokens.test.ts.
      </p>

      <h3 class="mt-10 font-display text-h5">Identity palette</h3>
      <ul class="mt-5 grid gap-3 sm:grid-cols-3 lg:grid-cols-5">
        {
          IDENTITY_TOKENS.map((t) => (
            <li class="rounded-lg border border-border p-3">
              <span
                class="block h-12 w-full rounded border border-foreground/20"
                style={`background: var(${t})`}
              />
              <code class="mt-2 block font-mono text-[0.6875rem] break-all">{t}</code>
            </li>
          ))
        }
      </ul>

      <h3 class="mt-10 font-display text-h5">The season line, one fixed date per season</h3>
      <ul class="mt-5 grid gap-2">
        {
          SEASON_FIXTURES.map(({ date, band }) => (
            <li class={`${band} px-6 py-4`}>
              <SeasonLine date={date} />
            </li>
          ))
        }
      </ul>

      <h3 class="mt-10 font-display text-h5">Arch frames</h3>
      <div class="mt-8 grid grid-cols-2 items-end gap-x-6 gap-y-8 md:grid-cols-4">
        <ArchFrame
          image={ARCH_FIXTURES.girls}
          shape="lancet"
          width={600}
          sizes="(min-width: 768px) 25vw, 50vw"
          alt={ARCH_FIXTURES.girls.alt}
        />
        <ArchFrame
          image={ARCH_FIXTURES.coneflowers}
          shape="lancet"
          width={600}
          sizes="(min-width: 768px) 25vw, 50vw"
          alt={ARCH_FIXTURES.coneflowers.alt}
          grade="warm"
          class="[--arch-ratio:100/190]"
        />
        <ArchFrame
          image={ARCH_FIXTURES.bibles}
          shape="door"
          width={600}
          sizes="(min-width: 768px) 25vw, 50vw"
          alt={ARCH_FIXTURES.bibles.alt}
        />
        <ArchFrame
          image={ARCH_FIXTURES.balcony}
          shape="door"
          width={600}
          sizes="(min-width: 768px) 25vw, 50vw"
          alt={ARCH_FIXTURES.balcony.alt}
          grade="warm"
          reveal={false}
          class="[--arch-ratio:100/118]"
        />
      </div>

      <h3 class="mt-10 font-display text-h5">Building glyphs</h3>
      <ul class="mt-5 flex flex-wrap items-end gap-8">
        <li class="text-season-green dark:text-mint">
          <BuildingGlyph name="window" class="w-12" />
        </li>
        <li class="text-gold-ink"><BuildingGlyph name="door" class="w-12" /></li>
        <li class="text-goal-purple dark:text-gold-light">
          <BuildingGlyph name="rose" class="w-12" />
        </li>
        <li class="text-goal-brown dark:text-gold-light">
          <BuildingGlyph name="basin" class="w-12" />
        </li>
        <li class="text-foreground"><BuildingGlyph name="window" class="w-16" /></li>
      </ul>

      <h3 class="mt-10 font-display text-h5">The rule button</h3>
      <p class="mt-2 max-w-[60ch] text-sm text-muted-foreground">
        Square, with the gold rule beneath. Hover or focus it: the rays spread.
      </p>
      <div class="mt-6 flex flex-wrap items-center gap-8">
        <CtaLink cta={RULE_CTA} variant="rule" />
        <CtaLink cta={SECONDARY_CTA} variant="rule" />
      </div>
    </div>
  </section>

  <section class="overflow-x-clip bg-season-green py-section-lg" aria-label="The watchword mark">
    <div class="mx-auto w-full max-w-content px-6">
      <SeasonLine date={new Date('2026-09-23T12:00:00Z')} />
      <div class="mt-10">
        <WatchwordMark size="band" />
      </div>
      <div class="mt-14">
        <WatchwordMark size="hero" />
      </div>
      <div class="mt-12 flex flex-wrap items-center gap-8">
        <CtaLink cta={RULE_CTA_DARK} variant="rule" onDark />
        <div class="flex items-end gap-6 text-mint">
          <BuildingGlyph name="window" class="w-10" />
          <BuildingGlyph name="door" class="w-10" />
          <BuildingGlyph name="rose" class="w-10" />
          <BuildingGlyph name="basin" class="w-10" />
        </div>
      </div>
    </div>
  </section>

  {/* scaffold: church */}
  <section class="bg-background py-section-lg">
    <div class="mx-auto w-full max-w-content px-6">
      <h2 class="font-display text-h3">Church blocks</h2>
      <p class="mt-2 max-w-[60ch] text-sm text-muted-foreground">
        The eight page-builder blocks the church site adds. Every fixture below is real church
        content, not lorem: the Sunday schedule, the three doors, two FAQ entries verbatim, Isaiah
        12:4, the building's 1929 date and the real giving link. The staff grid is the one exception
        on purpose, three synthetic people, because that block's data comes from Sanity in
        production.
      </p>
    </div>
  </section>

  {/* Task 7: temporary hero fixtures, see the note by the fixture data above. */}
  <Hero
    eyebrow={HERO_FULL_FIXTURE.eyebrow}
    headline={HERO_FULL_FIXTURE.headline}
    subhead={HERO_FULL_FIXTURE.subhead}
    backgroundImages={HERO_FULL_FIXTURE.frames}
    primaryCta={HERO_FULL_FIXTURE.primaryCta}
    secondaryCta={HERO_FULL_FIXTURE.secondaryCta}
    layout="full"
    size="short"
    headingId="styleguide-hero-full"
  />

  <Hero
    eyebrow={HERO_SPLIT_FIXTURE.eyebrow}
    headline={HERO_SPLIT_FIXTURE.headline}
    subhead={HERO_SPLIT_FIXTURE.subhead}
    backgroundImages={HERO_SPLIT_FIXTURE.frames}
    facts={HERO_SPLIT_FIXTURE.facts}
    primaryCta={HERO_SPLIT_FIXTURE.primaryCta}
    secondaryCta={HERO_SPLIT_FIXTURE.secondaryCta}
    layout="split"
    size="short"
    headingId="styleguide-hero-split"
  />

  <SundayTimes
    eyebrow={SUNDAY_TIMES_FIXTURE.eyebrow}
    heading={SUNDAY_TIMES_FIXTURE.heading}
    items={SUNDAY_TIMES_FIXTURE.items}
    doors={SUNDAY_TIMES_FIXTURE.doors}
    showMap={SUNDAY_TIMES_FIXTURE.showMap}
    headingId="styleguide-sundaytimes"
  />

  <Timeline
    eyebrow={TIMELINE_FIXTURE.eyebrow}
    heading={TIMELINE_FIXTURE.heading}
    rows={TIMELINE_FIXTURE.rows}
    surface="muted"
    headingId="styleguide-timeline"
  />

  <StaffGrid
    eyebrow={STAFF_GRID_FIXTURE.eyebrow}
    heading={STAFF_GRID_FIXTURE.heading}
    group={STAFF_GRID_FIXTURE.group}
    showBios={STAFF_GRID_FIXTURE.showBios}
    members={STAFF_GRID_FIXTURE.members}
    surface="background"
    headingId="styleguide-staffgrid"
  />

  <FaqBand
    eyebrow={FAQ_FIXTURE.eyebrow}
    heading={FAQ_FIXTURE.heading}
    items={FAQ_FIXTURE.items}
    headingId="styleguide-faq"
  />

  <ScriptureBand
    verse={SCRIPTURE_FIXTURE.verse}
    reference={SCRIPTURE_FIXTURE.reference}
    accentWord={SCRIPTURE_FIXTURE.accentWord}
    headingId="styleguide-scripture"
  />

  <HeritageBand
    eyebrow={HERITAGE_FIXTURE.eyebrow}
    heading={HERITAGE_FIXTURE.heading}
    body={HERITAGE_FIXTURE.body}
    image={HERITAGE_FIXTURE.image}
    headingId="styleguide-heritage"
  />

  <GiveBand
    heading={GIVE_BAND_FIXTURE.heading}
    body={GIVE_BAND_FIXTURE.body}
    buttonLabel={GIVE_BAND_FIXTURE.buttonLabel}
    buttonUrl={GIVE_BAND_FIXTURE.buttonUrl}
    headingId="styleguide-give"
  />

  <DocumentList
    eyebrow={DOCUMENT_LIST_FIXTURE.eyebrow}
    heading={DOCUMENT_LIST_FIXTURE.heading}
    docs={DOCUMENT_LIST_FIXTURE.docs}
    surface="muted"
    headingId="styleguide-documentlist"
  />

  <LinkCards
    eyebrow={LINK_CARDS_FIXTURE.eyebrow}
    heading={LINK_CARDS_FIXTURE.heading}
    cards={LINK_CARDS_FIXTURE.cards}
    surface="background"
    headingId="styleguide-linkcards"
  />

  <Hours
    eyebrow={HOURS_FIXTURE.eyebrow}
    heading={HOURS_FIXTURE.heading}
    settings={HOURS_FIXTURE.settings}
    surface="muted"
    headingId="styleguide-hours"
  />
  {/* scaffold:end */}
</BaseLayout>
